# Frontend environment variables
# Copy to .env.local for local development — never commit real values

# Live backend URL (set this in Vercel environment variables)
VITE_API_URL=https://your-backend.onrender.com
